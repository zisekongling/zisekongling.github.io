# 「青春时光」视觉/布局重做方案（v0.2 待确认 · 现代化可爱风）

> 范围：`beijing-time` 前端。目标 = **现代化 + 可爱**，且必须**适配课堂环境**（远看清晰、近看不闹、不分散注意力、动效克制）。
> 保留现有卡片质感（圆角 + 半透明 + 模糊 + 阴影）与响应式骨架，注入一个可爱但不喧闹的记忆点。

---

## 一、核心思路与视觉记忆点

**记忆点 = 「软糯玻璃 · 漂浮圆角」**（soft-glass × floating pill）

- 现代化可爱 ≠ 幼稚花哨。用**低饱和马卡龙色 + 大圆角 + 柔和阴影 + 轻盈漂浮动效**营造"软糯可爱"。
- 卡片升级为**软玻璃拟态**：半透明 + 顶部一道微光 + 柔和投影（两层阴影：近投影 + 远投影），边缘圆润（24–28px）。
- 时间数字保持**大而清晰**的现代感，数字用等宽栈 + `tabular-nums` 精确对齐，加一层极淡的圆润底块让时间"有轮廓"。
- 动效克制：一个轻量**呼吸式浮动**（卡片极小幅上下漂移）+ 按钮按压反馈；**完全尊重 `prefers-reduced-motion`**，课堂环境可一键安静。

**为什么不是模板答案**：不做"奶油米白+衬线+陶土""黑底+酸性绿""报纸 hairline"。马卡龙低饱和色板 + 软玻璃 + 漂浮，是从"青春/可爱"这个气质出发，但被"课堂"约束收敛——可爱而不闹。

---

## 二、课堂适配约束（硬性）

| 维度 | 约束 |
|---|---|
| 可读性 | 文字对比度 ≥ 4.5:1；时间数字超大号、加粗、无花哨干扰 |
| 色彩 | 低饱和马卡龙，避免高刺激荧光色；卡片底半透明不浑浊 |
| 动效 | 所有动效尊重 `prefers-reduced-motion`；默认浮动幅度小、无闪烁 |
| 专注 | 高考倒计时/金句不抢时间主角；装饰低调，不滚动、不弹跳 |
| 投影 | 柔和多层阴影，不用生硬黑边，远距离也能看清层级 |

---

## 三、布局调整（保留现有骨架）

```
┌──────────────────────────────────────────────┐
│ header：logo + 倒计时胶囊 + 控制按钮（圆润胶囊） │
├────────────────────────────┬─────────────────┤
│  time-section（主角·软玻璃） │ countdown-      │
│   圆润时间块 + 高考角标      │ sidebar         │
│  quote-section（金句）       │  （倒数日）       │
├────────────────────────────┴─────────────────┤
│ settings-panel（展开区，圆润化，保持现状逻辑）    │
└──────────────────────────────────────────────┘
```

- 桌面为主：时间卡大主角，金句卡次之，右侧倒数日栏固定宽。
- 高考倒计时 = 时间卡右上角**圆角胶囊徽章**，紧邻时间，保证"一眼"覆盖。
- 窄屏降级沿用现有断点（1024/900/768/600/480/360 + 横屏）：卡片圆角阶梯缩小、时间块缩格换行不溢出、金句字号收缩、间距用 `clamp()`。校验 320px 不溢出、可读。

---

## 四、统一色板逻辑（token 体系 · 低饱和马卡龙）

所有主题（新旧）共用一套语义变量并补齐缺失项：

```
--primary  主色（柔和主调）
--secondary 次色（渐变副调/装饰）
--accent   强调/点缀（金句高亮、毫秒）
--bg       页面底色（浅奶油/柔和）
--text     正文（保证对比度）
--card-bg  卡片底（半透明，var(--bg) 派生）
--shadow-near / --shadow-far  双层柔和阴影
--gaokao-color  高考胶囊专用
--progress-normal / --progress-warning
--radius    统一大圆角 token
```

**调和手法**：每个主题定一个**色相锚点**并整体降饱和（马卡龙化），统一亮度阶梯与半透明卡片底，补全进度条语义色，避免切换漂移。

---

## 五、主题规划

**保留重调（10 个）**：light/dark/ocean/forest/sunset/midnight/sakura/high-contrast/minimal/nature — 统一马卡龙降饱和 + 补全变量。

**新增（4 个，现代化可爱 · 课堂友好）**：

| 新增主题 | 类名 | 风格取向 | 主/次/强调色相锚点（低饱和） |
|---|---|---|---|
| 奶油 Dawn | `dawn-theme` | 奶油奶黄×薄荷，清晨元气 | 薄荷(#7fd8c0) / 奶油金(#ffd98e) / 珊瑚(#ff9a8b) |
| 糖果纸 Paper | `paper-theme` | 奶白纸感×淡蓝×柔粉，复古卷纸可爱化 | 淡蓝(#a8c8f0) / 奶白(#fff6ec) / 柔粉(#f6a6c0) |
| 星夜自习 StudyNight | `studynight-theme` | 深靛蓝×暖灯橙，晚自习温馨灯火 | 靛蓝(#4a5f8a) / 暖灯(#ffc98a) / 冰蓝(#9cc9f2) |
| 云霓 Nebula | `nebula-theme` | 淡紫×淡青渐变，青春梦幻 | 淡紫(#c4b5f0) / 淡青(#a8e0e8) / 柔粉(#f2b8d8) |

> 若觉得 4 个太多或风格不符，告诉我调整数量/名字。

---

## 六、改动点清单

| 文件 | 改动 |
|---|---|
| `css/variables.css` | 补全 token（加 `--shadow-near/far`、`--radius`、圆润底块色） |
| `css/themes.css` | 重调 10 主题马卡龙化 + 补全变量；新增 4 个主题类 |
| `css/layout.css` | 软玻璃卡片、大圆角、漂浮动效、高考胶囊徽章、clamp 间距 |
| `css/components.css` | 时间圆润底块/等宽/tabular、按钮胶囊按压、金句高亮 |
| `css/responsive.css` | 窄屏圆角/字号阶梯、防溢出校验 |
| `js/globals.js` | `themes` 数组、`themeDescriptions` 增加 4 个新主题 |
| `js/theme.js` | `applyTheme` classList.remove、`getThemeDisplayName` 增加新主题 |
| `index.html` | 主题选择区增加 4 个新主题卡（若为写死 HTML） |

> 不引入框架、不加网络字体依赖；纯 CSS/JS 局部改造，保持 Netlify 零构建。

---

## 七、风险与确认点

1. **可爱程度**：默认走"低饱和马卡龙 + 圆润 + 轻浮动"，克制课堂风。要不要再活泼一点（更多马卡龙点缀/更明显漂浮）？
2. **新增 4 个主题**名字/风格是否对上"现代化可爱课堂"气质。
3. **高考倒计时胶囊**移入时间卡内的位置，保留现有 `showGaokao` 开关。
4. 确认后是否**直接落地全部改动**？

确认方向后，我开始写代码。
