{
  "name": "bj-cors-proxy",
  "version": "1.0.0",
  "description": "bjtime CORS Proxy for Netlify (with image download & serve)",
  "dependencies": {
    "@netlify/blobs": "^1.1.0",
    "@netlify/functions": "^2.0.0",
    "node-fetch": "^2.6.7"
  },
  "scripts": {
    "build": "echo 'No build step required'"
  }
}
